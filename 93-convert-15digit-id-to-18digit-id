public String convert15to18DigitId(String fifteenDigit)
{
    if (fifteenDigit.length() != 15) {
        return '-1';
    }

    Id eighteendigit = Id.valueOf(fifteenDigit);

    return String.valueOf(eighteendigit);
}